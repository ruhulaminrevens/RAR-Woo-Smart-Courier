<?php
/**
 * RAR Woo Smart Courier uninstall handler.
 *
 * Settings and historical order metadata are intentionally preserved to avoid
 * destructive data loss if the plugin is temporarily removed or reinstalled.
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}
